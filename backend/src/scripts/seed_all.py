"""Ancien script de seed (SmartCity) — remplacé.

Utilise désormais le script de seed du package `backend` (portfolio) :
`backend.scripts.seed_all` (fichier : src/backend/scripts/seed_all.py).

Exemple d'utilisation :
    python -m backend.scripts.seed_all

Ce fichier est laissé comme wrapper informatif pour éviter les confusions.
"""

if __name__ == "__main__":
    print("[seed] Ce script est obsolète. Utilise : python -m backend.scripts.seed_all")
